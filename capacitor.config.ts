import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'ionic.fifa.stats',
  appName: 'Fifa Stats',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
